# Plan

1. Add a regression test proving a self-originated snapshot is not reapplied.
2. Mark snapshots as mounted before forwarding them to the parent.
3. Run focused and full tests plus typecheck/build.
4. Verify the signed-out browser flow keeps a non-A1 selection after autosave.

